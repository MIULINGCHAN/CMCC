package com.ocs.protocol.diameter.node;

class DefaultConnectionListener implements ConnectionListener
{
    public void handle(final ConnectionKey connectionKey, final Peer peer, final boolean b) {
    }
}
